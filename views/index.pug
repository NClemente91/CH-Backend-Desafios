html
    title Desafio 11 - Pug
    style
        include ../public/bootstrap/bootstrap.min.css
        include ../public/style.css
    body 
        div(class="contenedor")
            h1(class="title-vista") Vista de Productos
            div
                if productExist
                    table(class="table table-dark table-striped table-format")
                        thead
                            tr
                                th(scope="col") Nombre
                                th(scope="col") Precio
                                th(scope="col") Foto
                        tbody
                            each producto in productos
                                tr
                                    td=producto.title
                                    td=producto.price
                                    td
                                        img(src=`${producto.thumbnail}` alt="Imagen del producto" class="img-producto")
                else if !productExist
                    p(class="no-prod") No se encontraron productos